{
  "name": "jachwi-sunbae-landing",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "nodemailer": "^9.0.3"
  },
  "scripts": {
    "test": "node --test",
    "check": "node --test && node scripts/check-static.js"
  }
}
